name: Probe localtenniscourts.com

on:
  push:
    branches:
      - claude/great-wright-fn4b5c

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6.1.0
        with:
          python-version: '3.12'

      - name: Install deps
        run: pip install requests

      - name: Probe LTC
        run: python scripts/probe_ltc.py

      - name: Probe Better Admin for highbury-tennis slug
        run: |
          python scripts/probe_venue.py \
            islington-tennis-centre:tennis-court-outdoor \
            islington-tennis-centre:highbury-tennis \
            --days 2 --delay 5
